The project `FailureCriteria` is built on top of `MFrontGallery` which
provides the `cmake` infrastructure. The user may refer to the
documentation of this project to a comprehensive list of options: see
[this page]<https://thelfer.github.io/MFrontGallery/web/install.html>
for details.

This document only lists the most useful options.



Building
========

